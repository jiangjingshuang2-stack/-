# 早期 Diabetes 单项结果

该目录保存统一最终重跑之前生成的 Diabetes 收敛图，仅用于保留实验历史。

正式 Diabetes 数值、图片和结论请使用：

```text
results/final_rerun_20260606_172023/real_diabetes/
```

最终结果表明四种算法 test MSE 均约为 `0.475`，ADMM 仅使用 `11` 次迭代。该结论应与 Wine Red、Communities and Crime 以及合成数据主实验一起引用，用于说明 ADMM 在保持相近预测性能时通常具有更少迭代次数。
