# 早期 Wine Red 单项结果

该目录保存统一最终重跑之前生成的 Wine Red 收敛图，仅用于保留实验历史。

正式 Wine Red 数值、图片和结论请使用：

```text
results/final_rerun_20260606_172023/real_wine_red/
```

最终结果表明四种算法 test MSE 均约为 `0.599`，ADMM 仅使用 `12` 次迭代。它与 Diabetes 结果共同说明，ADMM 的迭代效率优势可以跨真实数据集重复出现。
